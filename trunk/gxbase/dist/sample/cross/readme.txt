cross

This example creates a single OpenGL window, and draws
a cross inside it. The cross resizes when the window is
resized, because the default implementation of OnResize()
automatically adjusts the glViewport() when the window
is resized.
